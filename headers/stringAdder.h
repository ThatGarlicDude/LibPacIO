#ifndef STRING_ADDER_H
#define STRING_ADDER_H

	#include <stdio.h>
	#include "filePathArray.h"

	void addString(FilePathArray* filePathArray, int index, const char* STRING);

	void addStrings(FilePathArray* filePathArray, const char* STRING);

#endif
